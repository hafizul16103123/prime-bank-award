import { RegistrationUnit } from "@/components/templates";
import { Suspense } from "react";

const Registration = () => {
	return (
		<Suspense>
			<RegistrationUnit />
		</Suspense>
	);
};

export default Registration;
